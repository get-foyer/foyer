{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "summary": {
      "type": "string",
      "description": "2-4 sentences of markdown describing what the agent is doing right now. Present tense, no preamble."
    },
    "topics": {
      "type": "array",
      "description": "3-6 research topics worth reading up on, derived from the agent's current work (libraries/APIs in play, concepts, error messages, unfamiliar terms). Extraction only — do NOT search the web. Skip the trivial/obvious. Keep stable vs the previous topics unless the focus has shifted. Empty array if nothing is worth suggesting.",
      "items": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string",
            "description": "A concise, searchable research topic (<=120 chars), e.g. \"React useTransition hook\"."
          },
          "reason": {
            "type": "string",
            "description": "One short line of provenance grounding the topic in the work (<=160 chars), e.g. \"you're editing App.tsx which uses useTransition\"."
          }
        },
        "required": ["topic", "reason"],
        "additionalProperties": false
      }
    },
    "primary": {
      "description": "Which ONE topic from `topics` is THE most valuable read for this task right now, or null. The topic must be the exact text of one item in `topics`. Return null when no pick is clearly valuable OR to keep the current primary unchanged (null = keep). The reason must be specific (<=80 chars), naming the area/doc that makes it primary — never generic filler.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "topic": {
              "type": "string",
              "description": "Exact text of one item in `topics`."
            },
            "reason": {
              "type": "string",
              "description": "Specific one-line why-now (<=80 chars), e.g. \"the session is editing server/providers + security hooks\"."
            }
          },
          "required": ["topic", "reason"],
          "additionalProperties": false
        },
        { "type": "null" }
      ]
    }
  },
  "required": ["summary", "topics", "primary"],
  "additionalProperties": false
}
